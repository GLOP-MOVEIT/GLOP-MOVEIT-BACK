meta {
  name: DeleteTrial
  type: http
  seq: 5
}

delete {
  url: {{URL}}/trials/1
  auth: bearer
}

auth:bearer {
  token: {{JWT_TOKEN}}
}

tests {
  expect response.status == 204 || response.status == 200 || response.status == 404
}
