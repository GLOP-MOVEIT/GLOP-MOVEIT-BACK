meta {
  name: Get results by trialId
  type: http
  seq: 1
}

get {
  url: {{URL}}/results/trial/:trialId
  body: none
  auth: bearer
}

params:path {
  trialId: 
}

auth:bearer {
  token: {{JWT_TOKEN}}
}

tests {
  expect(res.status).to.be.oneOf([200, 404]);
}
