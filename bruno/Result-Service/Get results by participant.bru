meta {
  name: Get results by participant
  type: http
  seq: 4
}

get {
  url: {{URL}}/results/participant/:participantId
  body: none
  auth: bearer
}

params:path {
  participantId: 
}

auth:bearer {
  token: {{JWT_TOKEN}}
}

tests {
  expect(res.status).to.be.oneOf([200, 404]);
}
