meta {
  name: GetSubscriptions
  type: http
  seq: 8
}

get {
  url: {{URL}}/subscriptions?userId=user123
  auth: bearer
}

auth:bearer {
  token: {{JWT_TOKEN}}
}

tests {
  expect(res.status).to.equal(200);
}
