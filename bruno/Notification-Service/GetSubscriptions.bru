test {
  expect response.status == 200
}
meta {
  name: GetSubscriptions
  type: http
  seq: 8
}

get {
  url: {{URL}}/subscriptions?userId=user123
  auth: bearer
}

auth:bearer {
  token: {{JWT_TOKEN}}
}

