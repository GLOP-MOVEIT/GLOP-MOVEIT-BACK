meta {
  name: ToggleSubscription
  type: http
  seq: 9
}

patch {
  url: {{URL}}/subscriptions/1/toggle
  auth: bearer
}

auth:bearer {
  token: {{JWT_TOKEN}}
}

tests {
  expect response.status == 200
}
