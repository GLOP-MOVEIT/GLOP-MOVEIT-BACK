meta {
  name: GetNotificationById
  type: http
  seq: 4
}

get {
  url: {{URL}}/notifications/1
  auth: bearer
}

auth:bearer {
  token: {{JWT_TOKEN}}
}

tests {
  expect(res.status).to.be.oneOf([200, 404]);
}
