meta {
  name: DeleteSubscription
  type: http
  seq: 10
}

delete {
  url: {{URL}}/subscriptions/1
  auth: bearer
}

auth:bearer {
  token: {{JWT_TOKEN}}
}

test {
  expect response.status == 204 || response.status == 200 || response.status == 404
}
