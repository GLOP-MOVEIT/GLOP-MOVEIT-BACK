meta {
  name: DeleteSubscription
  type: http
  seq: 10
}

delete {
  url: {{URL}}/subscriptions/1
  auth: bearer
}

auth:bearer {
  token: {{JWT_TOKEN}}
}

tests {
  expect(res.status).to.be.oneOf([204, 200, 404]);
}
