test {
  expect response.status == 200 || response.status == 204
}
meta {
  name: UpdateNotification
  type: http
  seq: 3
}

put {
  url: {{URL}}/notifications/1
  body: json
  auth: bearer
}

auth:bearer {
  token: {{JWT_TOKEN}}
}

body:json {
  {
    "title": "Updated Title",
    "content": "Updated notification Bruno"
  }
}
