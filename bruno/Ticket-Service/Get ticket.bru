meta {
  name: Get ticket
  type: http
  seq: 2
}

get {
  url: {{URL}}/tickets/:ticketId
  body: none
  auth: bearer
}

params:path {
  ticketId: 
}

auth:bearer {
  token: {{JWT_TOKEN}}
}

body:json {
  {
    "nickname": "Julien",
    "password": "password"
  }
}

test {
  expect response.status == 200 || response.status == 404
}
