test {
  expect response.status == 201 || response.status == 200
  expect response.body.id != null
}
meta {
  name: Create team
  type: http
  seq: 1
}

post {
  url: {{URL}}/teams
  body: json
  auth: bearer
}

auth:bearer {
  token: {{JWT_TOKEN}}
}

body:json {
  {
    "name": "Team Alpha"
  }
}