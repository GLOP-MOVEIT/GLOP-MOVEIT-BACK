meta {
  name: Add athlete
  type: http
  seq: 2
}

post {
  url: {{URL}}/teams/:teamId/athletes/:athleteId
  body: none
  auth: bearer
}

params:path {
  teamId: 
  athleteId: 
}

auth:bearer {
  token: {{JWT_TOKEN}}
}

tests {
  expect(res.status).to.be.oneOf([200, 201, 204, 404]);
}
