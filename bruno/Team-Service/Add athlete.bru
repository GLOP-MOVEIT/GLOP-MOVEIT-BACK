test {
  expect response.status == 200 || response.status == 201 || response.status == 204 || response.status == 404
}
meta {
  name: Add athlete
  type: http
  seq: 2
}

post {
  url: {{URL}}/teams/:teamId/athletes/:athleteId
  body: none
  auth: bearer
}

params:path {
  teamId: 
  athleteId: 
}

auth:bearer {
  token: {{JWT_TOKEN}}
}