test {
  expect response.status == 204 || response.status == 200 || response.status == 404
}
meta {
  name: Remove athlete
  type: http
  seq: 3
}

delete {
  url: {{URL}}/teams/:teamId/athletes/:athleteId
  body: none
  auth: bearer
}

params:path {
  teamId: 
  athleteId: 
}

auth:bearer {
  token: {{JWT_TOKEN}}
}