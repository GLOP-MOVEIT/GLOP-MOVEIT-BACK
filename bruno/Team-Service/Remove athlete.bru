meta {
  name: Remove athlete
  type: http
  seq: 3
}

delete {
  url: {{URL}}/teams/:teamId/athletes/:athleteId
  body: none
  auth: bearer
}

params:path {
  teamId: 
  athleteId: 
}

auth:bearer {
  token: {{JWT_TOKEN}}
}

tests {
  expect(res.status).to.be.oneOf([204, 200, 404]);
}
