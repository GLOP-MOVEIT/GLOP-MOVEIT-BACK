meta {
  name: Delete team
  type: http
  seq: 4
}

delete {
  url: {{URL}}/teams/:teamId
  body: none
  auth: bearer
}

params:path {
  teamId: 
}

auth:bearer {
  token: {{JWT_TOKEN}}
}

test {
  expect response.status == 204 || response.status == 200 || response.status == 404
}
