meta {
  name: UpdateTaskStatus
  type: http
  seq: 7
}

patch {
  url: {{URL}}/volunteer/tasks/1/status?status=IN_PROGRESS
  auth: bearer
}

auth:bearer {
  token: {{JWT_TOKEN}}
}

tests {
  expect(res.status).to.be.oneOf([200, 204]);
}
