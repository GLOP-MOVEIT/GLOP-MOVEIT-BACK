meta {
  name: DeleteTask
  type: http
  seq: 8
}

delete {
  url: {{URL}}/volunteer/tasks/1
  auth: bearer
}

auth:bearer {
  token: {{JWT_TOKEN}}
}

tests {
  expect response.status == 204 || response.status == 200 || response.status == 404
}
