meta {
  name: GetTasksByTaskTypeId
  type: http
  seq: 4
}

get {
  url: {{URL}}/volunteer/tasks/type/1
  auth: bearer
}

auth:bearer {
  token: {{JWT_TOKEN}}
}

tests {
  expect(res.status).to.equal(200);
}
