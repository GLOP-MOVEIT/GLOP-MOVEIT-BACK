meta {
  name: GetTasksByTaskTypeId
  type: http
  seq: 4
}

get {
  url: {{URL}}/volunteer/tasks/type/1
  auth: bearer
}

auth:bearer {
  token: {{JWT_TOKEN}}
}

test {
  expect response.status == 200
}
