meta {
  name: UpdateAssignmentStatus
  type: http
  seq: 6
}

patch {
  url: {{URL}}/volunteer/assignments/1/status
  body: json
  auth: bearer
}

auth:bearer {
  token: {{JWT_TOKEN}}
}

body:json {
  {
    "status": "ACCEPTED",
    "comment": "Candidature acceptée, bienvenue dans l'équipe"
  }
}

