meta {
  name: DeleteAssignment
  type: http
  seq: 7
}

delete {
  url: {{URL}}/volunteer/assignments/1
  auth: bearer
}

auth:bearer {
  token: {{JWT_TOKEN}}
}

test {
  expect response.status == 204 || response.status == 200 || response.status == 404
}
