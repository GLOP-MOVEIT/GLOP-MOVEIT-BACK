meta {
  name: DeleteAssignment
  type: http
  seq: 7
}

delete {
  url: {{URL}}/volunteer/assignments/1
  auth: bearer
}

auth:bearer {
  token: {{JWT_TOKEN}}
}

tests {
  expect(res.status).to.be.oneOf([204, 200, 404]);
}
