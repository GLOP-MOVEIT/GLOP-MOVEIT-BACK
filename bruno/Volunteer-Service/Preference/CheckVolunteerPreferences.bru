meta {
  name: CheckVolunteerPreferences
  type: http
  seq: 6
}

post {
  url: {{URL}}/volunteer/preferences/check
  body: json
  auth: bearer
}

auth:bearer {
  token: {{JWT_TOKEN}}
}

body:json {
  {
    "taskTypeId": 2,
    "volunteerIds": [2, 3, 4, 5]
  }
}