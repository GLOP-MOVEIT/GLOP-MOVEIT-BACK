test {
  expect response.status == 204 || response.status == 200 || response.status == 404
}
meta {
  name: DeletePreference
  type: http
  seq: 5
}

delete {
  url: {{URL}}/volunteer/preferences/1
  auth: bearer
}

auth:bearer {
  token: {{JWT_TOKEN}}
}

