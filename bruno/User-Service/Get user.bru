meta {
  name: Get user
  type: http
  seq: 2
}

get {
  url: {{URL}}/users/:id
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{JWT_TOKEN}}
}

body:json {
  {
    "nickname": "Julien",
    "password": "password"
  }
}

test {
  expect response.status == 200 || response.status == 404
}
