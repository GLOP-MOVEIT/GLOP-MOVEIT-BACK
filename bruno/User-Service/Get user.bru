meta {
  name: Get user
  type: http
  seq: 2
}

get {
  url: {{URL}}/users/:id
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: token
}

body:json {
  {
    "nickname": "Julien",
    "password": "password"
  }
}
