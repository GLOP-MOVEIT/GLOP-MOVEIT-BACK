meta {
  name: DeleteCompetition
  type: http
  seq: 5
}

delete {
  url: {{URL}}/championships/competitions/1
  auth: bearer
}

auth:bearer {
  token: {{JWT_TOKEN}}
}
tests {
  expect(res.status).to.be.oneOf([204, 200, 404]);
}