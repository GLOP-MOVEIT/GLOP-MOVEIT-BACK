meta {
  name: GetAllCompetitions
  type: http
  seq: 1
}

get {
  url: {{URL}}/championships/competitions
  auth: bearer
}

auth:bearer {
  token: {{JWT_TOKEN}}
}
tests {
  expect(res.status).to.equal(200);
}
