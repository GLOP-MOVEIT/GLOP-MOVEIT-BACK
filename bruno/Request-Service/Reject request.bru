test {
  expect response.status == 200 || response.status == 404
}
meta {
  name: Reject request
  type: http
  seq: 6
}

post {
  url: {{URL}}/requests/reject/:requestId
  body: json
  auth: bearer
}

params:path {
  requestId: 
}

auth:bearer {
  token: {{JWT_TOKEN}}
}

body:json {
  {
    "requestRejectionReason": "On ne veut pas de toi."
  }
}
