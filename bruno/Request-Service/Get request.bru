test {
  expect response.status == 200 || response.status == 404
}
meta {
  name: Get request
  type: http
  seq: 2
}

get {
  url: {{URL}}/requests/:requestId
  body: none
  auth: bearer
}

params:path {
  requestId: 
}

auth:bearer {
  token: {{JWT_TOKEN}}
}

body:json {
  {
    "nickname": "Julien",
    "password": "password"
  }
}
