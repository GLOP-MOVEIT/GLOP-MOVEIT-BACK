meta {
  name: Create volunteer request
  type: http
  seq: 4
}

post {
  url: {{URL}}/requests/volunteer/:userId
  body: json
  auth: bearer
}

params:path {
  userId: 
}

auth:bearer {
  token: token
}

body:json {
  {
    "coverLetter":"J'aimerais bien être volontaire."
  }
}
