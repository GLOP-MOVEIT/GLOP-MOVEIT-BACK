meta {
  name: Create volunteer request
  type: http
  seq: 4
}

post {
  url: {{URL}}/requests/volunteer/:userId
  body: json
  auth: bearer
}

params:path {
  userId: 
}

auth:bearer {
  token: {{JWT_TOKEN}}
}

body:json {
  {
    "coverLetterDocument":"J'aimerais bien être volontaire."
  }
}

tests {
  expect response.status == 201 || response.status == 200
}
